# 당근 중고거래 · 명품 매물 수집 가능여부 진단

2026-08-23 실측 (curl + Playwright, 로그아웃 상태).
대상: `www.daangn.com` 중고거래(fleamarket).

---

## 결론

**가능. 토큰·에뮬레이터·앱 후킹 전부 불필요.**

당근 중고거래 웹이 서버사이드 렌더링(Remix)이며, 매물 데이터가 HTML 안 `window.__remixContext` JSON에
그대로 실려 나온다. 인증 헤더 0개. GET 요청 + JSON 파싱이면 끝.

단, **전국 전수 수집에는 구조적 제약 3가지**가 있어 설계가 갈린다 (§3).

---

## 1. 접근 경로 (실측)

| 항목 | 값 |
|---|---|
| 목록/검색 | `GET /kr/buy-sell/?in=<동이름>-<regionId>&search=<키워드>` |
| 상세 | `GET /kr/buy-sell/<슬러그>-<nodeId>/` |
| 인증 | **없음.** authorization/device/서명 헤더 불필요. UA만 있으면 200 |
| 데이터 위치 | HTML 내 `window.__remixContext` → `state.loaderData` |
| 추가 필터 | `category_id`(21종), `price=<min>__<max>`, `only_on_sale` |

## 2. 획득 가능 필드 (실측 확인)

**목록 1건당** — id, href, 제목, **본문 전체**, 가격, 썸네일, 판매상태, 등록일시, 끌올일시, 지역명

**상세 1건당** — 위 항목 + 다음 전부:

| 필드 | 예시 | 리셀 활용 |
|---|---|---|
| `dbId` / `nodeId` | 995077233 | 중복제거 키 |
| `images[]` | 원본 6장 (1440px) | 진위판별·상태 확인 |
| `viewCount` | 267 | 수요 강도 |
| `chatCount` | 1 | 실거래 압력 |
| `favoriteCount` | 9 | 관심도 |
| `user.nickname / dbId` | 샬라스 / 41502576 | 판매자 추적 |
| `user.score` | 41.3 (매너온도) | 신뢰도 |
| `user.reviewCount` | 31 | 거래 이력 |
| `user.userArticles` | 판매자의 다른 매물 | **전문 리셀러 식별** |
| `region` | 부산 중구 중앙동 | 지역 시세차 |
| `category` | 여성잡화(31) | 분류 |
| `recommendedArticles` | 유사매물 목록 | **키워드 없이 그래프 탐색** |
| `user.webCrawlNotAllowed` | false | 크롤 거부 플래그 — 존중 필요 |

→ 시세 DB·저가 알림·판매자 프로파일링에 **필요한 데이터 전부 확보 가능**.

## 3. 제약 3가지 (설계 결정 요소)

### ① 검색이 동(洞) 단위 지역 스코프
전국 검색 옵션이 없다. `in=<동>-<id>` 필수이며 결과는 해당 동 매물만 반환.
→ **전국 = 동 전수 순회.** 전국 행정동/읍/면 약 3,600~4,500개.
   (정확한 동 목록은 지역 트리 스윕 1회 ~250요청으로 확보 가능)

### ② 1쿼리 최대 ~285건, 페이지네이션 없음
`page` 파라미터 무효(1페이지와 동일 응답). 실측 최대치 284건.
→ 결과가 상한을 넘는 조합은 초과분 유실. **카테고리·가격 구간으로 쿼리 분할** 필요.
   명품 키워드는 동 단위에서 대부분 상한 미만이라 실무 영향은 작음.

### ③ 일부 브랜드 단독 키워드가 0건 반환
실측: `샤넬` 0건 / `샤넬가방` 11건 / `CHANEL` 37건. `프라다` 0 / `프라다 가방` 11.
`디올` 0 / `디올 가방` 6. 반면 `구찌` 38, `에르메스` 17은 단독으로도 정상.
→ 원인 미확정(브랜드 키워드 필터 추정). **브랜드 × 접미어 확장 키워드셋으로 우회 가능** (검증됨).

## 4. 성능 실측

| 지표 | 값 |
|---|---|
| 응답 시간 | 중앙값 0.77s (동시 10 기준) |
| 부하 테스트 | **60요청 / 5초, 동시 10 → 전부 200. 429 없음** |
| 레이트리밋 헤더 | 없음 (노출된 제한 없음) |
| 실효 처리량 | 약 12 req/s (단일 IP, 동시 10) |

## 5. 규모 산정

키워드셋 = 브랜드 60개 × 접미어(가방/지갑/시계 등) → 실용 180~600 쿼리.

| 시나리오 | 요청 수 | 소요 (단일 IP, 12 req/s) |
|---|---|---|
| 수도권(약 2,000동) × 180키워드, 목록만 | 36만 | **약 8시간** |
| 전국(약 4,000동) × 180키워드, 목록만 | 72만 | 약 17시간 |
| 전국 × 600키워드 (풀 커버리지) | 240만 | 약 55시간 |
| 상세 확장 (매물 1건 = 1요청) | 수집건수만큼 | 명품 10만건 가정 시 +2.3시간 |
| **증분 모니터** (관심 300동 × 60키워드) | 1.8만 | **회당 약 25분** |

- 병렬 IP N개 → 시간 1/N 선형 단축.
- 초기 1회 풀수집 후에는 증분 모니터만 돌리면 되므로 운영 부담 낮음.

## 6. 준법 / 리스크 (중요)

`robots.txt` 실측:

```
User-agent: *
Disallow: /kr/buy-sell/s/*     ← 검색 결과 페이지 (차단)
```

- **`/kr/buy-sell/s/?search=` 경로는 robots 차단 대상.**
- **`/kr/buy-sell/?in=..&search=` 경로는 차단 목록에 없고, 당근이 자체 사이트맵
  (`sitemap-popular-keywords.xml`)에 직접 등재해 크롤을 유도하고 있음** → 이 경로 사용 권장.
- 상세 페이지 `/kr/buy-sell/<슬러그>/` 는 차단 없음. 당근 사이트맵에 36,226건 등재 확인.
- `anthropic-ai`, `GPTBot`, `Scrapy` 등 AI/크롤러 UA는 `/kr/` 전체 차단 → **일반 브라우저 UA 사용 필요.**
- 응답에 `user.webCrawlNotAllowed` 플래그 존재 → true인 판매자 데이터는 제외하는 게 안전.
- 당근 이용약관은 자동수집을 금지. IP/계정 제재 가능성 있음. 속도 조절·비공개 활용 권장.
- SSR 구조는 배포에 따라 바뀔 수 있음 → 파서 유지보수 포인트.

## 7. 권장 진행안

1. **PoC (0.5일)** — 브랜드 10개 × 수도권 100동 수집 → CSV. 실제 매물 밀도·가격 분포 확인.
2. **키워드셋 확정 (0.5일)** — ③ 제약 대응 확장 키워드 + 오탐(가품/부속품) 필터 룰.
3. **풀수집 (1일)** — 수도권 우선, 이후 전국.
4. **증분 모니터 + 시세 산출** — 25분 주기 폴링, 신규 매물 diff.

기존 레이트리밋·재시도·정규화 파이프라인은 그대로 재사용 가능.
